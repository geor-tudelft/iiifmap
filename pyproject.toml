[tool.poetry]
name = "iiifmap"
version = "0.1.0"
description = "Tools for handling IIIF scanned maps"
authors = ["GEOR"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
